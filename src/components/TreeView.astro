---
import TreeNodeItem, { type TreeNode } from "./TreeNodeItem.astro";

const { tree, activePath } = Astro.props as {
  tree: TreeNode[];
  activePath: string;
};
---

<ul class="tree">
  {tree.map((node) => (
    <TreeNodeItem node={node} activePath={activePath} />
  ))}
</ul>
